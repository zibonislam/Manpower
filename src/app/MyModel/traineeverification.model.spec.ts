import { Traineeverification } from './traineeverification.model';

describe('Traineeverification', () => {
  it('should create an instance', () => {
    expect(new Traineeverification()).toBeTruthy();
  });
});
